# 变更记录 2026-09-19

## 23:36 第四轮：阻断级修复与门禁重建（依据 CODE-REVIEW-20260919.md）

修 `CODE-REVIEW-20260919.md` 中的 P0 项与门禁失效问题。

**P0-1 程序无法启动（头号问题）**
- `lib/exif.js` 与 `cmd/cmd_shared.js` 的 3 处 `using` 声明式资源管理需要 Node ≥24，
  在 Node 22 上解析即报 SyntaxError，任何子命令都无法执行。改为共享单例 + 进程退出时释放。
- 顺带修 P1-5：exiftool 由「每文件一个实例」改为**模块级单例**，
  峰值子进程数从 cpus²（8 核即 64 个）降到恒定进程池。
- `engines` 由错误的 `>=22` 改为 `>=20`（实际下限由 `exiftool-vendored@35` 决定），
  README 同步；入口新增版本前置校验，把裸堆栈换成可读提示。

**P0-2 质量门禁三重失效**
- `eslint.config.js` 恢复 `no-unused-vars`/`no-empty`/`no-fallthrough`/`no-prototype-builtins`
  （此前全被关闭，导致 111 文件 0 问题），开启 `prettier/prettier`；错误数 178 → 0。
- `scripts/check_syntax.cjs` 扫描范围从 `cmd/`+`lib/` 扩到全仓库，
  并新增 engines 比对与「超出下限的新语法」扫描，防止 `using` 类问题复发。
- 新增 `.github/workflows/ci.yml`（Node 20/22/24 三档 × check + lint + test）。

**P0-3 Zip Slip 路径穿越**
- `cmd/cmd_zipu.js` 新增 `resolveSafeEntryPath()`：剥离盘符/绝对路径/UNC、逐段丢弃 `..`、
  用 `path.relative` 复核落点在解压目录内，越界条目丢弃并告警。此前 `../../evil.txt` 可写到任意目录。

**死代码与依赖清理**
- 删除零引用文件 `lib/cue-parse.js`、`lib/words.json`（939KB，随 npm 包分发）、`lib/notes.txt`
- 删除死函数：`findUniqueElement`、`uniqueByFields2`、`getDateTags`、`makePath`、`fixEncoding`、
  `convertNativeErr`、`FileEntry`、`unzipFileUseAdmZip`、`guessEncodingUseAdmZip`、QSV 预设等
- `cmd_remove.js` 的 `itemDesc` 累加链（8 处）从未被读取，实际生效的是 `fullItemDesc`，已删除
- 移除已无引用的 `adm-zip` 依赖与 import

**其它**
- `--version` 此前输出 `unknown`（yargs 从 cwd 查找 package.json 在全局安装时失败），改为直接读取
- `lib/core.js` 新增 `hasOwn()`，替换 7 处 `obj.hasOwnProperty()` 直调
- `cmd_run.js` 如实提示"未实现"，不再留下两个未使用的局部变量

验证：`npm run check` 78 文件全通过，`npm test` 43/43，`eslint` 0 error，`node index.js --help` 正常。

---

## 代码审查与高性价比缺陷修复

对 `media-cli.js` 完成一轮全面代码审查（功能缺陷、代码质量、工程架构、可维护性，不含安全议题），
并修复其中"改动小、收益大、风险低"的 19 项缺陷。

### 审查产出

- 整合两份独立审查报告的结论，形成单一事实来源：`docs/TODO-FIXES-20260919.md`
- 该文档按「已修复 19 项 / 待修复 21 项 / 有意搁置 6 项」归类，待修复项含位置、证据与修复建议

### 修复内容（19 项）

**数据正确性**

- `lib/helper.js`：`filenameSafe` 字符类中 `\\s` 误写为字面反斜杠+s，导致文件名中**所有小写字母 s 被静默删除**（`test.jpg`→`tet.jpg`）。修正为 `\s`
- `lib/helper.js`：`safeRemove` 补返回值（此前 `cmd_remove` 操作日志 `dest` 恒为 `undefined`）；失败时不再静默吞错；重名回退由单层改为递增后缀循环（此前第三次同名即失败且无提示）
- `lib/tools.js`：`md5Short` 由 `slice(limit)` 改为 `slice(0, limit)`，此前返回 24 字符而非 8
- `lib/encoding.js`：`RE_CHARS_MOST_USED` 模板串尾部残留正则定界符 `/`，导致该正则永不匹配，已删除

**功能缺陷**

- `cmd/cmd_dcim.js`：`ErrorTypes.PROCESS_ERROR`（不存在）替换为 `PROCESSING_FAILED`，恢复错误分类与退出码（2002）
- `cmd/cmd_move.js`：修正 dry-run 下 `movedCount` 与"已移动"日志无条件执行导致的统计造假
- `cmd/cmd_lr.js`：区分"跳过"与"失败"，修正 `operation.completed` 汇总数字
- `cmd/cmd_compress.js`：`--overwrite` 此前一路传递但从不校验（参数撒谎），改为 `pathExists && !f.overwrite`
- `cmd/cmd_remove.js`：补齐必需条件校验（此前 `--audio`/`--mtime`/`--ctime` 单独使用被错误拒绝）；引入有限正数校验，堵住 `--width abc`（NaN）绕过校验的路径
- `cmd/cmd_remove.js`：新增 `checkVideoParams()`，使此前声明却从不读取的 `--video` 成为有效条件
- `cmd/cmd_remove.js`：`--pattern` 尊重 `--regex` 开关；非法正则不再抛错后被静默跳过，改为降级为字面匹配并告警
- `cmd/cmd_zipu.js`：testMode 下 purge 补显式守卫（此前仅靠 `UnzipOneFile` 返回 `undefined` 的副作用侥幸安全）
- `cmd/cmd_ffmpeg.js`：修正 `acc + x || 0` 的运算符优先级问题（任一 duration 缺失即清零累计时长）
- `cmd/cmd_ffmpeg.js`：元数据时间戳由 12 小时制 `hh` 改为 `HH`

**结构性改进**

- `cmd/cmd_remove.js`：`checkConditions` 由 5 个布尔值穷举 32 种组合（约 90 行）重构为条件列表 + `every()`（18 行）。已用全部 1024 种输入组合暴力验证与原实现完全等价；`--video` 借此成为第 6 个条件而非新增 16 个分支
- `lib/debug.js`：`flushFileLog` 写入后清空缓存，避免重复 flush 导致日志双写累积
- `lib/core.js`：`parallel()` 改为操作数组副本，不再用 `splice` 修改调用方原数组

**国际化与工程配置**

- `lib/i18n.js`：补齐 9 个代码中使用但词典缺失的键（此前界面直接显示英文 key）
- `package.json`：新增 `engines.node >= 22`（代码使用 `using` 声明式资源管理）；`files` 白名单加入 `presets.yaml`（此前发布后用户拿不到预设文件）
- `test/test_helper.js`：新增 3 条回归测试，锁定 `filenameSafe` 保留字母 s、仍清除非法字符、`md5Short` 截断长度

**仓库卫生**

- `.gitignore`：忽略 Box-Agent 会话目录（`YYYY-MM-DD-xxxxxxxx/`、`.box-agent/`、`.box-agent-scratch/`）

### 验证

- `npm run check`：全部 `*.js` 通过
- `npm test`：28/28 通过（较修复前 +3，为新增回归测试）
- `cmd_remove` 端到端冒烟（dry-run）：`--sizel` 正确匹配、`--pattern` 正确匹配、非法正则明确告警、`--video` 被接受并参与判定
- 修复前文件备份于 `temp/backups/review-fix-20260919/`

### 备注

- 本次未处理 21 项待修复问题与 6 项有意搁置问题，详见 `docs/TODO-FIXES-20260919.md`
- `lib/exif.js`、`lib/ffmpeg_presets.js`、`cmd/cmd_ffmpeg.js` 中含审查期间已存在的在改内容（如调试用 `console.log`），一并提交

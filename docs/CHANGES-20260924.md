# CHANGES-20260924

## 2026-09-24 21:22:00 (GMT+8)

- **Electron 客户端解耦摄入与计划推演架构（Two-Phase Decoupled Pipeline）及 ShanaEncoder / HandBrake 级 UI/UX 强化**：
  - **架构方案实施**：根据对标 ShanaEncoder 与 HandBrake 生产级截图的深度剖析，落地完整方案文档 `docs/FFMPEG-ELECTRON-STAGED-INGESTION-DESIGN-20260924.md`。打破原“先调参再扫描创建计划”的阻塞耦合，重构为两阶段解耦流：Phase 1 媒体即时摄入与元数据并发探测（`staged`），Phase 2 纯内存毫秒级计划推演（`ready`）。
  - **全链路 IPC 契约扩展**：
    - `contracts.ts` 与 `ipc-channels.ts`：扩展 `TaskStatus` 支持 `'staged'`（待规划）；`PlanTask` 扩充 `audioCodec`、`bitrate`、`containerFormat` 与 `cmdPreview`；定义 `STAGE_INPUTS` 渠道与 `StageInputsResult`；
    - `preload/index.ts`：暴露 `window.api.stageInputs(paths)` 安全接口。
  - **主进程并发探测与规范路径去重**：
    - 在 `ffmpeg-service.ts` 中维护规范化路径去重集 `stagedEntries`；
    - 结合 8 工作池并发 `ffprobe` 提取视频/音频/时长/分辨率/容器格式；
    - 提供实时日志输出，遇到同名或已添加文件自动安全去重并反馈跳过数量。
  - **Pinia Store 任务管理增强**：
    - `plan.ts`：实现 `addStagedTasks()`、`removeTask()`、`removeSelectedTasks()`，以及计算属性 `totalDuration`、`totalSize`、`stagedCount`、`hasStaged`。
  - **TaskTable 与 UI/UX 视觉质感对齐业界标杆**：
    - **彩色容器格式徽章**：按文件扩展名渲染高辨识度彩色微标（MP4 蓝、MKV 紫、WEBM 绿、MOV 橙、AVI 粉、TS 靛蓝、其他灰）；
    - **待规划状态精准反馈**：未推演前目标列展示 `[待推演] 遵循左侧预设`，状态徽章展示 `待规划`；
    - **单行与批量删除控制**：每行右侧增设一键移除 `btn-remove-task`（`x` 图标）；
    - **底部对比与操作底板**：新增常驻底板（`table-bottom-bar`），左侧提供「全选 / 取消全选」与「移除所选 (N)」快捷按钮，右侧双列展示选中任务源媒体 vs 目标推演规格实时对比（或无选中时展示文件总数、总大小、总时长汇总）。
  - **多源即时摄入全域贯通**：
    - `ConfigPanel.vue`（文件选择、目录选择、手动路径、面板拖拽）、`GlobalDropMask.vue`（全局拖拽蒙层）、`HeroEmpty.vue`（空态双按钮）、`App.vue`（顶栏全局导入）添加文件后立即调用 `stageInputs` 提取元数据并瞬时呈现于任务列表；
    - `HeroEmpty` 瞬时淡出，右侧列表直接可见全部媒体，彻底解决“点击计划前对文件毫无感知”的用户痛点。
  - **HeaderBar 状态与按钮自适应**：
    - 检测到 `hasStaged` 时，计划按钮高亮并呈现「生成计划」，禁止在未推演时直接启动转码；状态栏实时提示「已摄入新媒体，请点击『生成计划』」；
  - **全量自动化测试与回归验证全绿**：
    - Playwright E2E 测试扩展增补两阶段管道即时探测、彩标、底板对比与任务删除用例，3/3 测试全部绿灯；
    - 根目录单元测试 326/326 全部通过，130 个 JS 语法校验通过，ESLint 0 警告 0 报错，TypeScript 类型检查零错误。

## 2026-09-24 21:05:00 (GMT+8)

- **Electron 客户端硬件监控、状态栏演进与交互优化**：
  - **HeaderBar 品牌冗余消除与硬件监控胶囊**：移除顶栏重复的“mediac FFmpeg Studio”品牌字样（统一由操作系统窗口标题栏承载），将左侧空间重构为侧边栏折叠按钮及 GPU、CPU 核心数、系统内存使用胶囊；常用操作按钮（生成/更新计划、开始转码、终止、清空）紧随硬件状态呈流式排布。
  - **常驻式底部状态栏 StatusBar**：新增 `StatusBar.vue` 紧凑常驻状态栏（高 24px），实时呈现 FFmpeg/FFprobe 二进制检测状态、硬件加速器分层（NVIDIA CUDA/Intel QSV/AMD/CPU）、转码进度与任务概览，以及实时日志抽屉快捷入口与错误计数。
  - **右侧任务列表操作精简与修复**：修复表格项中资源管理器文件夹定位按钮未转码前无响应的问题（未转码前准确定位并高亮源文件，转码后定位目标产物）；将无意义的默认单任务日志按钮替换为查看媒体参数与推演命令的 `btn-inspect-task` 信息按钮，仅在转码失败（`status === 'failed'`）时动态呈现鲜红错误日志直达按钮。
  - **全链路实时操作日志埋点**：
    - 服务端 `ffmpeg-service.ts`：在 `createPlan()` 生命周期中增加输入扫描分析、文件发现数量、计划构建耗时与预估总耗时的实时日志推送；
    - 前端 `ConfigPanel.vue`：对用户选择文件、选择目录、手动添加路径、拖拽导入、移除输入项、切换转码预设，以及微调视频尺寸/CRF/码率/音频参数等全生命周期动作进行 live 日志捕获与呈现。
  - **批处理习惯对齐**：左侧 Dropzone 默认点击行为从单选/多选文件升级为选择目录（`pickDirectory`），并提供明确的「添加文件」与「添加目录」双按钮。
  - **高级选项收归应用设置面板**：将硬件加速方式、解码模式、音频并发数、覆盖产物、动漫调优、严格模式与高危删除源文件选项从左侧配置栏剥离，完整迁移至 `SettingsModal.vue`（设置弹窗），侧栏仅保留输入输出、预设选择、视频微调、音频微调 4 张轻量核心卡片。
  - **全量自动化测试与回归核验通过**：Playwright E2E 冒烟与交互测试 2/2 通过，单元测试 326/326 全部通过，130 个 JS 语法校验通过，ESLint 0 警告 0 报错。

## 2026-09-24 19:35:51 (GMT+8)

- **修复 Electron UI 10 项用户痛点与交互缺陷**：
  - **日志面板文字对比度恢复**：将日志控制台独立为固定深色高对比终端主题（`#0d1117` 背景 + `#e6edf3` 文字 + `#7ee787` 绿色 CMD），彻底解决明亮模式下因黑底黑字导致日志不可见的问题。
  - **Inspector 命令行高亮与转义重构**：修复 `previewCmd` 路径与参数含空格及 CJK 字符时缺少引号导致 shell 解析错误的问题；以单 pass 正则词法分析器重写 `highlightFfmpegCmd`，杜绝引号路径与 flag 正则的交叉破坏，代码框改用高对比终端配色。
  - **整合 TitleBar 与 HeaderBar**：窗口标题栏统一显示 `mediac FFmpeg Studio · 批量音视频转码工作台`，移除 HeaderBar 内重复的中文字符；HeaderBar 直接承载常用操作按钮（生成计划、开始转码、终止、清空、参数变更告警）与状态胶囊，消除主面板冗余工具栏，释放主视图垂直空间。
  - **左侧配置栏收起与悬浮展开**：配置栏顶部增加收起按钮与快捷键（Ctrl+B），主视区左边缘增设 docked 浮动展开按钮。
  - **输出目录打开行为修复**：新增 `system:open-path` IPC 渠道，`ExecutionBoard` 改用 `window.api.openPath` 直接进入输出目录，避免原 `shell.showItemInFolder` 打开并高亮父级目录的 Windows 默认行为。
  - **全域明暗主题对比度大幅提升**：全面重写 `theme.css` 设计令牌，明亮模式采用 `#ffffff` 纯白输入框与 `#d0d7de` 清晰边框，暗黑模式采用 GitHub Dark 级高对比，开启系统级字体抗锯齿 `-webkit-font-smoothing: antialiased`，杜绝文字发虚。
  - **音频低码率档位扩充与格式修复**：音频码率下拉框补充 `48k`（Opus 语音推荐）与 `64k`（Opus 音乐推荐）；修复预设比特率计算将 `96` 误拼为 `96000k` 的格式化缺陷。
  - **预设列表参数简洁呈现**：预设下拉选项支持格式化展示核心参数（如 `hevc_2k [HEVC · 1440p · CRF23 · AAC 128k]`）。
  - **默认界面主题改为明亮模式**：默认采用 `light` 主题，保留本地用户选择持久化。
  - **设置弹窗工具路径文件选择器**：为 `ffmpeg`、`ffprobe` 与 `mediainfo` 路径增加「浏览...」文件选择器按钮。
  - **定制原生系统菜单栏**：规划高价值生产级菜单（文件、任务、视图、帮助），并在 Vue 前端响应对应快捷键与菜单命令动作。


- **落地 Electron 客户端高保真生产级 UI 与 Playwright E2E 自动化测试**：
  - **核心组件体系实施**：按照 UI/UX 规范 (v1.3.0) 与架构文档 (v1.2.0)，基于 Vue 3 + Pinia + 原型 CSS 设计令牌完成全套生产级组件：
    - `GlobalDropMask.vue`（全域拖拽半透明蒙层）
    - `HeaderBar.vue`（顶栏环境状态徽章、GPU 探测与侧栏/设置/主题切换）
    - `HeroEmpty.vue`（零任务空态引导画板）
    - `ConfigPanel.vue`（左侧 5 张卡片：输入/输出、预设分组渲染、视频/音频微调与脏标记撤销、高级选项）
    - `TaskTable.vue`（任务大列表：全选/单选、状态指示、行双击呼出检查器、定位与单任务日志直达）
    - `TaskInspectorDrawer.vue`（右侧 460px 滑出抽屉：媒体流参数对比、高亮命令行一键复制）
    - `LogDrawer.vue`（右侧 480px 日志抽屉：等级过滤、单任务 ID 聚焦过滤）
    - `SettingsModal.vue`（系统设置：暗黑/明亮主题持久化、工具路径配置、重新检测）
    - `ExecutionBoard.vue`（底部紧凑/展开执行控制台：实时倍速、进度与 ETA、产物目录直达）
    - `App.vue`（主界面组装、IPC 事件流分发、计划生成、执行终止与快捷键响应）
  - **状态机与健壮性加固**：
    - 实现平滑 `STALE` 状态机：参数微调即时点亮 `dot-dirty` 与 `undo-btn`，触发工具栏「更新计划」脉冲高亮与告警横幅，绝不粗暴清空已分析好的任务列表；
    - 在 `preload` 与 `App.vue` 增设安全深拷贝序列化，根治 Vue Proxy 穿透 `contextBridge` 触发的 `An object could not be cloned` 致命隐患；
    - 支持系统级防休眠锁（`powerSaveBlocker`）、任务栏实时进度联动、系统通知与安全同步进程清理。
  - **Playwright E2E 自动化测试全绿**：
    - 在 `apps/mediac-desktop/tests/e2e/` 建立 `playwright.config.ts` 与 `_electron.launch()` 夹具；
    - 编写并运行 `01-smoke.spec.ts`（启动、布局、主题、侧栏折叠验证）与 `02-interaction.spec.ts`（输入添加、计划生成、检查器抽屉、STALE 状态机更新、日志与设置抽屉联动）；
    - 全量验证通过：E2E 测试 2/2 通过，根目录单元测试 326/326 全部通过，`npm run check` 130 个 JS 语法校验通过，`npm run lint` 0 error。

## 2026-09-24 18:02:15 (GMT+8)

- **完善 Electron 架构方案并增补 Playwright 测试规范与风险审查**：
  - **架构方案升级 (v1.2.0)**：在 `docs/FFMPEG-ELECTRON-ARCHITECTURE-FINAL-20260924.md` 中增补第 10 节《前端自动化测试规范（基于 Playwright）》，落实 `_electron.launch()` 夹具、环境隔离与五大核心用例规范，并在第 12 节增加架构风险审查摘要。
  - **第二轮架构审查报告**：新增 `docs/FFMPEG-ELECTRON-ARCHITECTURE-REVIEW-20260924-180500.md`，深度甄别并给出解决方案：跨目录引用第三方依赖致 `package:win` 安装包闪退（P0）、架构文档 UI 规格滞后（P1）、`before-quit` taskkill 异常中断（P1）与海量文件 IPC 序列化压力（P2）。

## 2026-09-24 17:50:18 (GMT+8)

- **完成 Electron 客户端 UI/UX 方案升维与高保真交互 Demo 重构**：
  - **设计方案升级 (v1.3.0)**：在 `docs/FFMPEG-ELECTRON-UI-UX-DESIGN-FINAL-20260924.md` 中补充「全域拖放蒙层 + 零配置 Hero 空态」、「参数微调脏标记（Dirty State）与一键撤销（Undo）」、「右侧抽屉式 TaskInspectorDrawer 取代底部常驻命令框（释放 100% 垂直高度）」、「平滑 STALE 状态机」与「失败任务日志直达聚焦过滤」设计。
  - **交互原型落地**：全面重构 `ffmpeg-ui-demo/mediac-desktop-ui.html`，实现上述全部交互动效与状态流，修复命令高亮双重转义，无缝兼容暗黑/明亮主题；基于真实 Electron 渲染生成并核准全新桌面态、检查器抽屉、脏状态告警与空态引导全套实机截图（位于 `ffmpeg-ui-demo/_shots/`）。

## 2026-09-24 16:23:17 (GMT+8)

- **修复 Electron Analyze/Events 和基础 UI 可用性**：
  - **IPC**：所有返回值和 EngineEvent 经过可克隆安全序列化，修复 `An object could not be cloned`。
  - **核心**：Electron service 改用共享 Scan/Planner/Task，复制 bundled preset，目录输入、outputMode、override、strict、delete-source 与 CLI/ffweb 对齐。
  - **UI**：增加输入文件/目录、输出目录、preset、outputMode、fps、speed、dimension、视频/音频码率、override、strict、删除源文件确认、Analyze/Start/Stop、任务和事件显示。
  - **真实验证**：Electron renderer CDP 流程单文件和目录 Analyze/Start 均完成，事件序列可序列化；`npm test` 326/326、check 130、lint、Electron typecheck/build 通过。

## 2026-09-24 16:17:30 (GMT+8)

- **完成 FFmpeg Electron 桌面端 UI 与交互方案设计**：
  - **文档**：新增 [FFMPEG-ELECTRON-UI-DESIGN-20260924.md](file:///c:/Home/Projects/media-cli.js/docs/FFMPEG-ELECTRON-UI-DESIGN-20260924.md)，明确采用 `Naive UI (Dark Theme) + VueUse` 架构路线。
  - **内容**：详尽论证 UI 选型优劣考量（淘汰纯手写、Headless UI 与 Vuetify 3），规划双栏工作台布局（Header 状态栏、ConfigPanel 配置栏、TaskTable 任务表格、ExecutionBoard 看板、LogTerminal 日志终端），定义状态机与安全确认机制，并打通任务栏进度、防休眠锁、系统通知等 Electron 原生桌面特性。

## 2026-09-24 15:53:22 (GMT+8)

- **修复 WebUI stale plan 窗口**：
  - **行为**：开始新一次 planning 即清理旧 `currentPlan`、summary 和进度；失败的请求不能误执行上一份计划。
  - **测试**：新增 WebUI stale-plan 回归；全量 `npm test` 326/326、`npm run check` 130 个 JS 文件、`npm run lint` 通过。

## 2026-09-24 15:51:45 (GMT+8)

- **统一 CLI/ffweb 输入扫描规则**：
  - **Scan**：WebUI 改用共享 `scanWebInputFiles`，复用 preset 类型过滤、include/exclude/extensions、start/count 和 filelist 语义。
  - **测试**：新增 Web scan 过滤/filelist parity 回归；ffweb 真实计划、执行、override、stop、全跳过流程继续通过。
  - **验证**：定向测试通过；全量门禁在本阶段提交前再次执行。

## 2026-09-24 15:44:46 (GMT+8)

- **完成 CLI/ffweb 共享 Planner 与行为统一**：
  - **Planner**：新增统一的任务准备、内部 Plan、空计划/全跳过状态、plan identity 和源文件安全删除 helper；CLI 与 ffweb 共同使用 `buildCliTask`。
  - **行为**：ffweb 接入显式 retry/delete-source 确认、override 安全提交、共享删除校验和 stale-plan 防护；CLI 使用同一删除 helper。
  - **结果**：统一 `SKIP_REASON`，空计划和全跳过计划可进入 Engine summary；新增 CLI/ffweb canonical task parity、Engine 空/全跳过、删除确认和 ffweb 回归测试。
  - **验证**：`npm test` 324/324、`npm run check` 129 个 JS 文件、`npm run lint` 全部通过；ffweb 真实转码/override/stop/全跳过回归通过。

## 2026-09-24 15:17:24 (GMT+8)

- **调整当前实施范围**：
  - 优先完成 CLI/ffweb 共享核心和真实功能回归。
  - Electron 的 ffmpeg 分发、许可 notices、Authenticode 签名、NSIS/Portable 干净机验收暂缓，不作为当前阶段门槛。

## 2026-09-24 15:15:59 (GMT+8)

- **完成阶段 1E skipReason 与 EngineEvent 统一**：
  - **Result**：统一 `SKIP_REASON` 值域，preflight skipped task 保留在 Plan 中。
  - **Engine**：已标记 skipped 的 task 不启动 ffmpeg，直接进入 skipped summary。
  - **ffweb**：透传 EngineEvent，计划预览跳过无效 task。
  - **验证**：Engine/task/ffweb 定向测试 17/17 通过；全量回归将在本阶段提交前确认。

## 2026-09-24 15:00:28 (GMT+8)

- **完成阶段 1D 稳定 runFFmpeg 接口**：
  - **Result**：新增 `runFFmpeg` 稳定结果包装，底层 `runFFmpegCmd` 继续作为迁移适配器。
  - **接入**：CLI、ffweb 和 Electron Engine 改为消费稳定 RunResult，不再直接把可变 entry 传给上层。
  - **验证**：Engine/ffweb 定向测试通过；Electron `npm run typecheck` 通过；`npm test` 已通过（316/316）。

## 2026-09-24 14:44:23 (GMT+8)

- **开始阶段 1B/5C：稳定 AttemptResult 与 AbortSignal**：
  - **Result**：新增 `TaskAttemptResult`，Engine 的 attempt done 事件不再直接暴露可变 entry。
  - **取消链路**：mediainfo/ffprobe、硬件能力探测、任务构建和 output commit 增加 AbortSignal 传递/检查。
  - **测试**：增加 task signal 透传和 AttemptResult 契约测试；修复 ffweb SSE 测试超时句柄和 WebServer.close 清理；`npm test` 通过（316/316）；`npm run check` 通过（126 个 JS 文件）；`npm run lint` 通过。

## 2026-09-24 14:22:24 (GMT+8)

- **修复 Electron 打包/开发启动 Error 对话框**：
  - **根因**：Electron 将共享核心 bundle 到 `out/main` 时未复制 `lib/hanzi_*.txt` 数据文件，`lib/unicode.js` 启动时读取 `hanzi_rarely.txt` 失败。
  - **修复**：electron-vite 增加核心数据文件复制插件；补充 main startup log、preload/renderer 错误日志和 `ensure:electron`。
  - **调试**：新增 `dev:debug`、README；`npm run dev`、`npm run build`、`npm run package:win` 和 `win-unpacked/MediaCli.exe` 直接启动均通过。
  - **已知提示**：`music-metadata/file-type` 依赖触发 Vite `eval` 警告，不影响当前启动；后续可单独处理。

## 2026-09-24 14:04:51 (GMT+8)

- **完成 FFmpeg/FFprobe 配对环境解析**：
  - **Resolver**：新增 `resolveFFprobeBinary`，支持 `FFPROBE_PATH/FFPROBE_BINARY` 和选中 ffmpeg 同目录优先。
  - **MediaInfo**：支持显式 `ffprobePath`，Electron 计划和硬件探测使用同一配对环境。
  - **验证**：ffprobe resolver 测试通过；Electron `npm run typecheck` 通过。
  - **未包含**：ffprobe 版本/能力一致性校验、preset resources 运行时加载、cwd preset 禁用和 cache 按 binary/version 隔离。

## 2026-09-24 13:58:11 (GMT+8)

- **完成 Electron 阶段 5B 临时文件恢复**：
  - **Manifest**：在 Electron `userData/active-tasks.json` 原子记录 runId、taskId、tempPath、outputPath 和 createdAt。
  - **恢复**：应用启动按 manifest 精确清理受管临时文件；成功、失败、取消和退出时清理 manifest，不再扫描固定 `%TEMP%` 目录。
  - **验证**：Electron `npm run typecheck`、`npm run build` 通过；Engine 生命周期测试通过。
  - **未包含**：Job Object 强杀、ffprobe 媒体探测超时/取消、manifest 多进程锁和正式崩溃演练。

## 2026-09-24 13:54:50 (GMT+8)

- **完成 Electron 阶段 5A 进程守护桥接**：
  - **Engine**：透传 task 的 onSpawn/onExit 生命周期回调，并覆盖 attempt/task 上下文。
  - **Electron service**：登记和注销活动 PID；Stop 使用 `execFile(taskkill /T /F)` fallback，不经 shell、不阻塞主进程；before-quit 触发清理。
  - **验证**：Engine 生命周期测试通过；Electron `npm run typecheck`、`npm run build` 通过。
  - **未包含**：Job Object、强制崩溃级清理、mediainfo/ffprobe AbortSignal、session manifest 恢复。

## 2026-09-24 13:50:31 (GMT+8)

- **完成 Electron 阶段 4F 打包验证**：
  - **打包**：`npm run package:win` 成功生成 NSIS 和 Portable 产物，main/preload/renderer 均完成构建。
  - **依赖修正**：移除 `file:../..` 本地 `mediac` 依赖，Electron 直接 bundle 共享核心，避免把整个仓库和递归 app release 打入安装包。
  - **验证**：发现并修复了首次打包中 `node_modules/mediac/apps/mediac-desktop/release/win-unpacked/electron.exe` 递归打包问题。
  - **未包含**：ffmpeg/ffprobe 分发、正式代码签名、Job Object、干净 Windows 机器验证和安装包 E2E。

## 2026-09-24 13:43:09 (GMT+8)

- **完成 Electron 阶段 4E 交互增强**：
  - **Renderer**：增加拖拽文件、输出目录选择、进度条、执行事件刷新和计划状态同步。
  - **验证**：`npm run typecheck`、`npm run build` 通过。
  - **未包含**：完整参数面板、preset 编辑、renderer reload 恢复、ffprobe 配对和 Job Object。

## 2026-09-24 13:41:23 (GMT+8)

- **完成 Electron 阶段 4D 最小可用 UI**：
  - **Renderer**：新增环境信息、媒体文件选择、Analyze、Start/Stop、PublicPlanSnapshot 任务列表和 EngineEvent 展示。
  - **交互**：renderer 通过 preload 受控 API 调用 plan/start/stop/snapshot，并订阅执行事件。
  - **验证**：`npm run typecheck`、`npm run build` 通过。
  - **未包含**：拖拽、输出目录设置、参数面板、进度条、renderer reload 恢复、托盘和正式安装包运行验证。

## 2026-09-24 13:39:51 (GMT+8)

- **完成 Electron 阶段 4C Engine 桥接**：
  - **Service**：Electron main service 接入共享 Options、Scan、Task、PlanSnapshot 和 Engine。
  - **IPC**：新增 plan/create、execution/start、execution/stop、execution/snapshot 和 EngineEvent 通道；preload 提供对应受控 API。
  - **生命周期**：支持 AbortController 停止、before-quit dispose 和主进程向 renderer 推送 EngineEvent。
  - **验证**：`npm run typecheck`、`npm run build` 通过。
  - **未包含**：ffprobe 配对解析、renderer reload 快照恢复、完整媒体操作 UI、Job Object 和正式 Windows 安装包验证。

## 2026-09-24 13:34:58 (GMT+8)

- **完成 Electron 阶段 4B 安全 IPC/环境骨架**：
  - **IPC**：新增 shared contracts/channels，preload 暴露 webUtils 和受控 API；main 统一校验 sender、运行时参数、CSP、导航/新窗口和权限。
  - **环境**：新增 ffmpeg/preset/hardware environment service，Electron 通过本地 `mediac` 依赖复用核心模块；dialog 支持文件/目录选择。
  - **验证**：`npm install`、`npm run typecheck`、`npm run build` 通过。
  - **未包含**：ffprobe 配对解析、Engine 任务 IPC、sender runtime schema 全量覆盖、ffweb 状态恢复和正式打包。

## 2026-09-24 13:30:41 (GMT+8)

- **完成 Electron 阶段 4A 工程骨架**：
  - **独立应用**：新增 `apps/mediac-desktop`，不改变根 CLI 包的 `main/bin/files`。
  - **安全基础**：配置 electron-vite、Vue/TypeScript、sandbox preload、contextIsolation、CSP、导航/新窗口限制和 shared contracts。
  - **基础能力**：preload 暴露 `webUtils.getPathForFile` 窄接口，main 提供版本和原生文件选择 IPC；builder 配置 preset extraResources、NSIS/Portable 目标。
  - **验证**：`npm install`、`npm run typecheck`、`npm run build` 均通过；生成 app package-lock。
  - **未包含**：Engine IPC 接入、sender/runtime 参数校验、ffmpeg/ffprobe 环境服务、Job Object、完整 UI 和正式安装包验证。

## 2026-09-24 13:26:21 (GMT+8)

- **完成 FFmpeg 共享核心阶段 2E CLI Options 适配**：
  - **Options**：CLI 计划阶段改用 `normalizeCliOptions` 和 legacy argv projection，统一 mode、jobs、speed、dimension、outputMode、strict、override 等字段。
  - **兼容性**：保留 CLI 的 input/directories/show-presets/info/doit/auto-confirm 字段和 `--ffargs` 优先级。
  - **验证**：真实 CLI dry-run 通过；`npm test` 通过（312/312）；`npm run check` 通过（122 个 JS 文件）；`npm run lint` 通过。
  - **未包含**：prepare 阶段完全移出 CLI、stale plan、delete-source Engine 化和统一 skipReason，留待后续阶段。

## 2026-09-24 13:22:44 (GMT+8)

- **完成 FFmpeg 共享核心阶段 2D 统一 CLI 执行**：
  - **Engine**：扩展 `ffmpeg_engine` 支持 confirmed retry/Attempt、attempt hooks 和 retry summary。
  - **CLI**：`runFFmpegTasks` 删除独立 pMap/retry 编排，改用共享 Engine；CPU fallback 通过 `buildCliTask` 重建任务，delete-source 保留 CLI 适配。
  - **验证**：真实 CLI dry-run 通过；`npm test` 通过（312/312）；`npm run check` 通过（122 个 JS 文件）；`npm run lint` 通过。
  - **未包含**：prepare concurrency 统一、skipReason 值域、delete-source Engine 化和 stale plan 处理，留待后续阶段。

## 2026-09-24 13:16:56 (GMT+8)

- **完成 FFmpeg 共享核心阶段 2C 统一任务构建**：
  - **Task**：`lib/ffmpeg_task.js` 新增可注入的 `buildCliTask`，承接原 CLI `prepareFFmpegCmd` 的媒体探测、目标路径、字幕、临时文件、strict skip 和任务字段构建。
  - **CLI 接入**：`cmd/cmd_ffmpeg.js` 删除私有 `prepareFFmpegCmd`，首次执行和 CPU retry 均调用共享任务构建器。
  - **验证**：新增 CLI/WebUI task seam 测试；`npm test` 通过（311/311）；`npm run check` 通过（122 个 JS 文件）；`npm run lint` 通过。
  - **未包含**：skipReason 统一值域、TaskAttempt/retry Engine、delete-source 统一和完整 CLI prepare 编排，留待后续阶段。

## 2026-09-24 13:11:12 (GMT+8)

- **完成 FFmpeg 共享核心阶段 2B 统一扫描管线**：
  - **Scan**：`lib/ffmpeg_scan.js` 接管单文件/目录/额外目录、filelist、去重、媒体类型、扩展名/include/exclude 和 start/count。
  - **CLI 接入**：`cmd/cmd_ffmpeg.js` 删除私有输入收集实现，改用共享扫描管线。
  - **验证**：新增 filelist/切片/过滤测试；`npm test` 通过（309/309）；`npm run check` 通过（122 个 JS 文件）；`npm run lint` 通过。
  - **未包含**：完整 CLI task 抽取、retry/delete-source 统一和 Engine prepare 阶段，留待后续阶段。

## 2026-09-24 13:06:19 (GMT+8)

- **完成 FFmpeg 共享核心阶段 2A 执行 Engine**：
  - **Engine**：新增 `lib/ffmpeg_engine.js`，统一任务队列、并发校验、取消、状态转换、RunResult、summary 和结构化事件。
  - **WebUI 接入**：`ffweb/task_runner.js` 不再维护独立执行循环，改为调用共享 Engine，继续通过原有 SSE 事件适配前端。
  - **测试**：新增 Engine 单元测试；`npm test` 通过（307/307）；`npm run check` 通过（122 个 JS 文件）；`npm run lint` 通过。
  - **未包含**：prepare/retry/delete-source 完整语义、CLI 迁移和 Electron 工程，留待后续阶段。

## 2026-09-24 12:59:26 (GMT+8)

- **完成 FFmpeg 共享核心阶段 1A 领域契约与适配层**：
  - **Options**：新增 `lib/ffmpeg_options.js`，统一 CLI/WebUI 选项规范化、mode、jobs、speed、dimension、outputMode、strict、override 和 ffargs 优先级。
  - **Plan/Event**：新增 InternalExecutionPlan/PublicPlanSnapshot 和结构化 EngineEvent，给任务分配稳定 id，避免将内部 argv、Stats、Set/Map、Error 直接暴露给前端。
  - **WebUI 接入**：ffweb 计划创建改用规范化 Options 和 PublicPlanSnapshot；任务构建、扫描和 RunResult 过渡适配保持可测试。
  - **验证**：`npm test` 通过（304/304）；`npm run check` 通过（120 个 JS 文件）；`npm run lint` 通过；ffweb 真实流程和契约测试通过。

## 2026-09-24 12:33:27 (GMT+8)

- **完成 FFmpeg 重构与 Electron 实施计划评审及第一批安全/行为修复**：
  - **P0 WebUI 安全**：ffweb API/SSE 增加 token 校验、Origin 校验、请求体大小限制，并拒绝非 loopback 监听；前端请求自动携带认证信息。
  - **P0 PowerShell 注入**：Windows 原生对话框不再把用户 title 拼入 PowerShell 源码，改由环境变量传递并清理控制字符。
  - **P0 停止语义**：WebUI 取消任务正确区分 cancelled/failed，等待 session 清理，pending task 标记 cancelled；硬件 probe 和 ffmpeg 执行支持 AbortSignal。
  - **P0 override**：已有目标默认提前跳过；`--override` 改为临时文件成功后再安全替换，替换失败保留旧目标。
  - **测试 seam**：新增可注入的 `lib/ffmpeg_scan.js` 与 `lib/ffmpeg_task.js`，并补充扫描、任务构建、认证、取消和 override 测试。
  - **验证**：`npm test` 通过（297/297）；`npm run check` 通过（116 个 JS 文件）；`npm run lint` 通过；WebUI 流程、扫描/任务/安全定向测试通过。

## 2026-09-24 09:46:00 (GMT+8)

- **完善与修正 FFmpeg Electron GUI 实施规划方案 (`docs/FFMPEG-ELECTRON-GUI-PLAN-20260924.md`)**：
  - **P0 任务编排层解耦**：正视 `cmd/cmd_ffmpeg.js` 中私有 `prepareFFmpegCmd` 与任务扫描构建逻辑无法直接被 GUI 复用的缺陷，提出独立抽取 `lib/ffmpeg_orchestrator.js` 让 CLI/GUI 共用一份核心调度器，补齐工时至 5~7 天；
  - **P0 Windows 孤儿进程防御重构**：补齐单纯依赖 Node.js 退出信号钩子在 Windows 任务管理器强杀/崩溃下失效的盲区，引入 Windows 进程树硬杀（`taskkill /T /F`）与 Job Object（`KILL_ON_JOB_CLOSE`）双层防御机制及启动自检；
  - **P1 修复状态判断与并发丢失**：修复原示例代码中未读取 `runFFmpegCmd` 返回值而误判 `entry.ffmpegFailed` 的 Bug；引入 `p-limit` 恢复并增强 `--jobs` 并发转码支持；
  - **P1 消除 `any` 类型声明**：补全 `IHardwareCaps`、`IPlanOptions`、`ITaskEntry`、`RunnerState` 等强类型接口契约。

## 2026-09-24 09:36:00 (GMT+8)

- **新增 FFmpeg Electron GUI 实施规划方案 (`docs/FFMPEG-ELECTRON-GUI-PLAN-20260924.md`)**：
  - 深入评估 Electron 在多媒体桌面工具场景下的工程确定性与优势，对比 WebUI、C# WPF 及 Tauri 方案；
  - 详细审计现有 7,500 行代码库，明确 `lib/` 核心转码与硬件降级矩阵模块 100% 零修改复用路径；
  - 给出 `electron-vite` + `Vue 3` + `TailwindCSS` + `electron-builder` 的全套最佳技术选型、IPC 协议定义、长任务防休眠与任务栏进度集成代码，以及 5 天路线图。

## 2026-09-24 09:14:00 (GMT+8)

- **原生系统文件夹/文件选择对话框穿透置顶（解决“点浏览无反应”）**：
  - 根因排查：在 Windows 10 环境下，从 Node.js 调起 PowerShell 的 `FolderBrowserDialog` / `OpenFileDialog` 若未提供所有者窗口（Owner Window Handle），操作系统默认将弹窗置于底层，在浏览器或 Edge App 全屏/前台时被遮挡在背后无法被视觉察觉，表现为“点击无反应”。
  - 核心修复：
    - `ffweb/dialog.js` 中创建隐藏的 `$form = New-Object Windows.Forms.Form; $form.TopMost = $true; $form.Activate(); $form.BringToFront();` 并将其作为 Owner 传入 `ShowDialog($form)`，强制对话框突破层级穿透置顶于浏览器正前方；
    - 改用 UTF-16LE 转 Base64 的 `-EncodedCommand` 参数调用 PowerShell，彻底杜绝命令行多行换行、引号和中文路径转义解析失效的问题；
    - 补齐异常日志记录，防止错误被静默吞没。
  - 前端反馈增强：
    - `ffweb/assets/app.js` 为「选择文件」、「浏览目录」和「输出目录」按钮添加点击时的 `⏳ 打开中...` 禁用加载中状态与交互反馈，并在发生异常时弹出友好的错误提示。

## 2026-09-24 09:03:00 (GMT+8)

- **流级元数据保护与多轨语言/标题完整保留**：
  - 根除预设 `streamArgs` 中有害的 `-map_metadata:s:v 0:s:v` 与 `-map_metadata:s:a 0:s:a`，统一改为全局 `-map_metadata 0`；并在 `buildStreamArgs` 中加入防御性正则过滤。
  - 恢复 FFmpeg 默认的 per-stream 元数据流映射机制，解决此前显式指定单个流元数据导致所有其它音频轨和字幕轨的语言代码 (`lang` / `language`) 与标题 (`title`) 被彻底丢弃的问题。
- **清除重编码流过期统计标签（根治 MediaInfo 虚高码率）**：
  - 在 `buildMetaArgs` 中对重编码的视频轨与音频轨显式清空 MKV/mkvmerge 遗留的旧统计标签：`BPS=`、`NUMBER_OF_BYTES=`、`NUMBER_OF_FRAMES=`、`_STATISTICS_TAGS=`、`_STATISTICS_WRITING_APP=`、`_STATISTICS_WRITING_DATE_UTC=`。
  - 彻底解决转码产物在 MediaInfo 中依然读取源视频旧的 `BPS=9386897` 导致显示 9000+ kbps 的问题，使播放器与分析工具准确显示实际转码码率。
- **WebUI 日志镜像落盘与即时 Flush**：
  - `ffweb/task_runner.js` 将所有 WebUI 日志通过 `log.fileLog` 同步写入会话文本日志文件；
  - 在任务批次完成、单任务结束、中止及 WebServer 关闭时显式调用 `log.flushFileLog()`，保证日志即时落盘至操作系统临时目录（`%TEMP%\mediac`）。
- **自动化测试补充**：
  - 在 `test/test_ffmpeg_params_v2.js` 中新增针对流元数据保护、BPS 清理以及 streamArgs 过滤的自动化单测，全绿通过。

## 2026-09-24 08:52:00 (GMT+8)

- **ffweb 前端预设展示增强**：
  - 后端 `/api/env` 扩展返回预设完整关键属性（`videoCodecFamily`, `audioCodec`, `videoQuality`, `videoBitrate`, `maxBitrate`, `audioBitrate`, `dimension`, `framerate`, `speed` 等）。
  - 前端预设下拉框列表项动态展示关键摘要（如 `hevc_2k [HEVC | AAC | Q:25 | 1920p | Max:7M]`），并在下拉框下方动态渲染彩色属性徽章卡片。
- **转码参数 Enum 选项扩充**：
  - 界面新增常用 Enum 选择器：`FPS`（默认/24/25/30/60/120）、`播放速度`（原速/1.25x/1.5x/2.0x/0.75x/0.5x）、`视频码率`（原画/1M/2M/4M/6M/8M/12M/16M 及自定义）、`音频编码`（跟随预设/copy/aac/opus/mp3/flac）、`音频码率`（96k/128k/192k/256k/320k）。
  - `task_runner.js` 完善参数转换与校验，将界面选项规范化传递给底层 ffmpeg plan 构建器。
- **转码过程透明化（Prepare 与 FFmpeg 命令行日志）**：
  - `lib/ffmpeg_run.js` 在执行命令前通过 `onLog` 输出 `[PREPARE] ...` 及实际调用的完整 `[CMD] ffmpeg ...`，任务完成输出 `[DONE] ...` 耗时与体积。
  - 前端控制台日志对 Prepare（薄荷绿）与 Command（亮蓝高亮）进行独立标签分类和高亮展示。
- **自动化测试健全**：
  - `test/test_ffweb_flow.js` 扩展全链路测试，验证预设元数据返回、自定义参数计划生成及 SSE 实时接收 `[PREPARE]` / `[CMD]` 日志事件。

# 🌐 中文本地化使用指南

## 📖 概述

本项目已添加完整的国际化（i18n）支持，目前支持中文和英文两种语言。系统会自动检测系统语言设置，也可以手动配置。

## 🔧 基本用法

### 在 JavaScript 文件中使用

```javascript
import { t } from "../lib/i18n.js"

// 基本翻译
const message = t('file.not.found')
// 输出: "文件未找到" (中文) 或 "File not found" (英文)

// 带参数的翻译
const messageWithParams = t('commands.lrmove.moved', { 
  src: '/path/to/source', 
  dst: '/path/to/dest' 
})
// 输出: "已移动: /path/to/source 到 /path/to/dest"
```

### 语言键名规范

- `commands.*` - 命令相关的描述和消息
- `file.*` - 文件操作相关消息
- `error.*` - 错误消息
- `success.*` - 成功消息
- `status.*` - 状态消息
- `confirm.*` - 确认消息
- `help.*` - 帮助信息
- `app.*` - 应用程序信息

## 🌍 语言检测

系统按以下优先级检测语言：

1. **环境变量** (`LANG`, `LANGUAGE`, `LC_ALL`)
2. **操作系统** (Windows 默认中文，其他系统根据环境变量)
3. **默认** (英文)

## ⚙️ 手动设置语言

```javascript
import { i18n, Languages } from "../lib/i18n.js"

// 设置为中文
i18n.setLanguage(Languages.ZH_CN)

// 设置为英文
i18n.setLanguage(Languages.EN_US)

// 强制使用中文
i18n.useChinese()

// 强制使用英文
i18n.useEnglish()
```

## 📝 添加新的翻译

### 1. 在 `lib/i18n.js` 中添加翻译键

```javascript
// 在 resources[ZH_CN] 和 resources[EN_US] 中都添加
'my.new.message': '我的新消息',
'my.new.message.en': 'My new message',
```

### 2. 在代码中使用

```javascript
const message = t('my.new.message')
```

## 🎯 已本地化的内容

### ✅ 已完成

- [x] `cmd_lr.js` - lrmove 命令的所有输出
- [x] 主程序信息（版权、描述等）
- [x] 错误处理系统消息
- [x] 通用操作消息
- [x] 文件操作消息

### 📋 待完成

- [ ] 其他命令模块的本地化
- [ ] 命令行参数描述的本地化
- [ ] 详细帮助文档的本地化

## 🔍 调试技巧

### 检查当前语言

```javascript
import { i18n } from "../lib/i18n.js"
console.log('当前语言:', i18n.getLanguage())
console.log('是否支持中文:', i18n.isChineseSupported())
```

### 临时切换语言进行测试

```javascript
// 保存当前语言
const originalLang = i18n.getLanguage()

// 临时切换到中文测试
i18n.useChinese()
console.log(t('file.not.found')) // 应该输出中文

// 切换回原来的语言
i18n.setLanguage(originalLang)
```

## 🎨 本地化最佳实践

### 1. 使用描述性键名
```javascript
// ❌ 避免
'error1': '错误消息'

// ✅ 推荐
'file.processing.failed': '文件处理失败'
```

### 2. 合理使用参数
```javascript
// ❌ 避免
`文件 ${filename} 处理失败`

// ✅ 推荐
t('file.processing.failed', { filename: filename })
```

### 3. 保持消息简洁
```javascript
// ❌ 避免
'这是一个非常长的错误消息，包含了很多不必要的信息，让用户很难理解到底发生了什么'

// ✅ 推荐
'文件处理失败，请检查文件格式'
```

### 4. 提供操作建议
```javascript
// 在错误消息后提供建议
log.showRed(t('file.not.found'))
log.showYellow(t('please.check.path'))
```

## 🧪 测试本地化

### 测试中文输出
```bash
# 设置中文环境
set LANG=zh_CN.UTF-8
node scripts/media_cli.js lrmove --help
```

### 测试英文输出
```bash
# 设置英文环境  
set LANG=en_US.UTF-8
node scripts/media_cli.js lrmove --help
```

## 📚 相关文件

- `lib/i18n.js` - 国际化系统核心
- `cmd/cmd_lr.js` - 本地化示例
- `ERROR_HANDLING.md` - 错误处理文档

## 🎁 示例输出对比

### 中文输出
```
==============================================================
media_cli.js lrmove <input> [output]

将 RAW 目录下的 JPEG 输出文件夹移动到其他位置

文件未找到: 输入路径不存在: /nonexistent/path
请检查文件路径是否正确
```

### 英文输出
```
==============================================================
media_cli.js lrmove <input> [output]

Move JPEG output of RAW files to other folder

File not found: Input path does not exist: /nonexistent/path
Please check if the file path is correct
```

---

💡 **提示**: 本地化系统会自动适应系统语言，无需手动配置。在中文系统上会显示中文，在英文系统上会显示英文。
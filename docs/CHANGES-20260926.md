# CHANGES-20260926

## 2026-09-26 18:38 — 修复 CI `desktop:lint` 因缺依赖失败

`npm run lint` 串联了 `desktop:lint`，但根仓库未配置 npm workspaces，CI 的
`npm ci`（仅装根依赖）不会安装 `apps/mediac-desktop` 的 devDependencies，
导致 `apps/mediac-desktop/eslint.config.js` 导入 `eslint-plugin-vue` 时
`ERR_MODULE_NOT_FOUND`、整条 lint 失败、CI 退出码 1。

- `.github/workflows/ci.yml`：在根 `npm ci` 之后、`lint` 之前新增
  `npm ci --prefix apps/mediac-desktop`，显式安装桌面端依赖
  （其 `package-lock.json` 已含 `eslint-plugin-vue` / `typescript-eslint` /
  `vue-eslint-parser` / `@eslint/js`，锁与 package.json 同步，`npm ci` 可用）。
- 未改用 workspaces：避免把 `electron` 等重型 devDependency 拖入根 `mediac`
  包的安装与发布链路，也避免重写根 `package-lock.json`。

### 验证

- 本地 `node --test` 两个测试文件 27/27 通过；`eslint` 两个测试文件 0 Error。
- 逻辑：CI 装上 desktop 依赖后 `desktop:lint` 即可解析 `eslint-plugin-vue`。

---

## 2026-09-26 17:33 — 修复 3 个非跨平台测试（CI 在 ubuntu-latest 上失败）

GitHub CI 跑在 `ubuntu-latest`（Linux），而 3 个用例写死了 Windows 语义，导致 `npm test` 退出码 1。
本次**只改测试、不改业务代码**，使用例在 Windows 与 Linux 均通过。

- `test/test_ffmpeg_bin_caps.js`：`resolveFFprobeBinary` 的兄弟节点探测文件名由 `ffprobe.exe`
  改为随 `process.platform` 变化（`win32` 用 `.exe`，其余平台不带后缀），写入与断言同步使用该常量。
  （根因：`src/transcode/ffmpeg_bin.js` 仅在 win32 追加 `.exe`，非 Windows 上找不到 `ffprobe.exe` 而返回 null。）
- `test/test_ffmpeg_task.js`：去掉写死的盘符绝对路径 `C:\out\...` / `C:\media\...` 与反斜杠断言，
  改用 `path.resolve` / `path.join` 构造期望路径，与源码 `ffmpeg_task.js` 的 `path` 计算保持一致。
  （根因：POSIX 下 `C:/out` 被视为相对路径、分隔符为 `/`，与 Windows 行为不同。）

### 验证

- 本地（Windows）`node --test test/test_ffmpeg_bin_caps.js test/test_ffmpeg_task.js`：27/27 通过
  （含此前失败的 3 例）；逻辑改为基于 `path` 后 Linux CI 也会得到一致结果。
- 仅改动 2 个测试文件，其余 334 例不受影响。

---

## 2026-09-26 17:19 — hwaccel.js 等大文件的拆分遗留清理

依据对 `src/transcode/` 大文件的实证核对（`FFMPEG-REVIEW-20260926` §B3/B2）。全部为删除与注释修正，
**不改任何编码/滤镜/分层行为**，最终 argv 由现有 335 个用例与真机 dry-run 双重保护。

### 一、删除死代码（全仓库含 test 逐一 grep 确认 0 消费者）

`src/transcode/hwaccel.js` 由 1606 → 1338 行：

- **legacy 质量标定链整条移除**：`QUALITY_OFFSET` 表、`TIER_TO_CALIB_KEY`、`qualityOffsetOf()`、
  `normalizeQuality()`（约 115 行）。2026-09-23 起编码器质量改走 VMAF 等值表
  （`VMAF_QUALITY_OFFSET` + `normalizeQualityForEncoder`），旧的三段阶梯表已无任何调用方。
- 移除 `bitDepthOf()`（位深判定实际由 `depthClassOf` + `needsDepthAlign` 承担）、
  `pickEncoder()`（`buildEncoderArgs` 已内联矩阵查表）、`buildEncoderArgsForSource()`
  （空壳 wrapper）、`needsComplexFilter()`（探测链已改 `-vf`+`-af`）、`HwAccel` 常量对象
  （`--hwaccel` 取值判定在 `hwdetect.normalizeHwaccelName`）。
- 移除 27 键的 `export default {...}`：所有调用方均为具名导入。
- 同步修正 `lib/media_parser.js` 中指向已删除 `bitDepthOf` 的注释（改指 `needsDepthAlign`）。

### 二、修正 4 处孤儿 / 错位注释

- `hwaccel.js`：`pickEncoder` 上方两份连体 JSDoc（首份仍声明已不存在的 `pixFmt`/`bitDepth` 参数）、
  描述已删除函数 `tierImpl()` 的悬空注释、`gpuBlocksDecode` 上方「主入口：按层级链…」悬空块。
- `ffmpeg_build.js`：`buildScaleFiltersFromPlan` 的文档块被错挂在 `depthAlignNeeded` 上方，
  真正的主人反而没有文档 —— 已各归其位。
- `hwaccel.js` 文件末尾的「对接说明」草稿段整体删除：其引用已全部失效
  （`cmd_ffmpeg.js 约 1957-2005 行` —— 该文件现 788 行；`lib/ffmpeg_presets.js` 已迁 `src/transcode/`；
  `PRESET_HEVC_SPEED` 全仓库仅剩该注释自身提及）。仅保留两条真实未完成项（AMF 未实测、探测超时未压测）。

### 三、补上探测缓存失效通道（真实缺陷，非整洁问题）

- `hwdetect.detectHardwareCapabilities` 是**不带 ffmpegPath 键**的模块级单例，
  `hwaccel.probeCache` 的键也**有意不含** ffmpegPath；而两个清理函数
  `clearHwCapabilitiesCache()` / `clearProbeCache()` 此前在 src/cmd/apps/test 中 **0 调用**。
  桌面端「设置 → 工具路径」改 ffmpeg 后，会把 `this.hardware` 置空重新探测，
  却仍拿回**旧二进制**缓存的能力（编码器集合、版本、GPU 代次）与分层结论。
- `ffmpeg_run.setFFmpegPath()` 现为唯一失效入口：路径**变化**时清空两份缓存，
  同值调用直接返回（不白白重探）。`clearHwCapabilitiesCache()` 同时清掉 in-flight promise，
  避免进行中的旧探测在结束时把旧结果写回。
- 新增 `test/test_ffmpeg_cache_invalidation.js` 覆盖该契约（已实测：去掉失效逻辑即失败）。

### 验证

- `npm test` 335/335（334 + 新增 1）；`npm run lint` 根 + 桌面端 0/0；`npm run check` 125 文件通过；
  `npm run desktop:typecheck` 0 错误。
- 真机 dry-run（`data/videos/TEST2__av1_10bit_1080.mkv` + `hevc_2k`）仍产出
  `-hwaccel cuda … -c:v hevc_nvenc -rc vbr -tune hq -cq 28 -b:v 0 -maxrate 8000K -bufsize 8000K`，与改动前一致。

### 仍遗留

- `hwaccel.js` 物理拆分（1338 行）尚未做，`ARCHITECTURE-MAINTENANCE-PLAN` 的 R6/R7 也**没有覆盖
  transcode 内的大文件**（R6 是 CLI lazy loading，R7 只列了 `cmd_remove/pick/rename`）。
- 位深判定仍有 4 套同源实现（`hwaccel.needsDepthAlign` / `media_parser.bitDepthOfFormat` /
  `gpu.bitDepthOfPixFmt` / `ffmpeg_build.depthAlignNeeded` 薄壳），R7 第 4 条未落地。

---

## 2026-09-26 16:27 — 评审清单 B7 剩余项收尾

- **`log.fileLog` 占位符错位**（`ffmpeg_run.js`）：失败日志里本应打印预设名，实际写成
  `entry.dstArgs?.dstAudioBitrate || entry.preset.name` —— 目标音频码率一旦有值，日志就出现一串数字，
  无法据此定位预设。改为固定取 `entry.preset.name`。
- **`selectTier` 对 0×0 源尺寸抛错**（`hwaccel.js`）：源宽高缺失（纯音频轨、探测失败的容器）时
  `calcLongEdge` 抛 `invalid source size 0x0`，被上层包装成费解的 `plan: ...`。现与 `calculateDstArgs`
  的 `> 0` 保护对齐：尺寸不可用时 `size = null`（不缩放），`probeLayer` 的缓存键按 0 记录，
  `buildScaleFilter` 本就支持 `size=null`（只做格式对齐）。实测 0×0 输入返回 `tier=cuda, size=null` 而非抛错。
- **`entry.useCUDA` 死字段**：全仓库无任何读取方，移除写入（此前会让任务快照/日志多一个误导性字段）。
- **日志抽屉的 DEBUG 噪音**（`stores/log.ts`、`LogDrawer.vue`）：`logStore.append` 此前把 `DEBUG` 强转为
  `INFO`，而 `ConfigPanel` 为 5 个微调字段各挂了 watcher → 拖动 CRF/码率滑杆会灌入大量"普通"INFO 行。
  现 `DEBUG` 成为真实级别（下拉框可单独筛选，灰色斜体），默认视图（"全部（隐藏 DEBUG）"）不再展示。
- **`deleteCompletedSources` 的 `includeExisting` 差异**：经复核并非"同一调用两端不一致"——
  两端**执行后**的删源行为一致，CLI 另有一条**执行前**针对「产物已存在」任务的删源路径，桌面端没有。
  删除源文件不可撤销，新增该路径属于功能新增而非一致性修补，故不改行为，只在代码里注明差异与原因。

验证：根 + 桌面端 lint 0/0；`npm test` 334/334；桌面端 typecheck 0 错误；CLI 常规路径 dry-run 命令与改动前一致。

---

## 2026-09-26 10:47 — 桌面端 UI 调整 + 接入 TS/Vue ESLint 门禁

### 一、系统信息面板（AboutModal）

- **加宽**：`.modal` 由 580px 调整为 780px（`max-width: 94vw`），路径与硬件列表不再被大量截断。
- **显示 ffmpeg / ffprobe 版本**：`EnvironmentSummary` 新增 `ffmpegVersion` / `ffprobeVersion`；
  ffmpeg 版本复用能力探测结果（`caps.version`），ffprobe 版本由 `ffprobe -hide_banner -version`
  单独探测一次并缓存（切换自定义 ffprobe 路径时失效重探）。面板改为「工具名 + 路径 + 版本徽章」三列布局。

### 二、顶栏与设置

- **移除顶栏 GPU 胶囊**（`HeaderBar.vue`）：它依赖异步的 env 探测结果，首帧为空、探测返回后才有内容，
  导致整条顶栏（含右侧操作区）启动时横向跳动。硬件/加速信息统一由状态栏与「关于」面板展示。
  同时清理随之失效的 `gpuText` 计算属性、`useEnvStore` 引入与 `.hw-pill`/`.hw-name`/`.dot`/`.gpu-pill` 死样式。
- **精简并发任务数说明**：由「视频与音频均按此并发；1 = 串行（视频建议保持 1，避免多路硬编会话抢占）」
  缩短为「1 = 串行，视频建议 1」。

### 三、桌面端接入 ESLint（TS + Vue）

- 新增 `apps/mediac-desktop/eslint.config.js`（flat config）：`js.configs.recommended` +
  `typescript-eslint` recommended + `eslint-plugin-vue` **flat/essential**（只取能发现真实缺陷的规则，
  不引入含排版主张的 `vue/recommended`，避免数百条纯格式告警淹没真实问题）。
  环境区分：renderer 用 browser globals，main/preload/shared/tests 用 node globals，`scripts/*.cjs` 按 CommonJS 处理。
- 新增脚本：桌面端 `lint` / `lint:fix`；根 `desktop:lint` / `desktop:lint:fix`，
  并把根 `lint` 改为 `eslint . --ext .js && npm run desktop:lint`（一条命令覆盖两端）。
  根配置忽略 `apps/mediac-desktop/**`（该目录有自己的配置，避免根 Prettier 规则误判其 TS/Vue 格式）。
- 依赖：`eslint@^10`、`eslint-plugin-vue@^10`、`vue-eslint-parser@^10`、`typescript-eslint@^8`、`@eslint/js@^10`、`globals@^17`。

### 四、修复的 lint 问题（50 warning + 8 error → 0 / 0）

**真实缺陷（error 级）**

- `ExecutionBoard.vue` / `SettingsModal.vue`：未使用的 `formatSize` / `computed` 引入。
- `TaskTable.vue`：`catch (e: any)` 中 `e` 未使用 → 改为可选 catch 绑定；
  `emit("clearAll")` 声明后从未使用 → 右键「清空任务列表」改为复用顶栏 `clearAll` 的唯一实现（消除重复代码）。
- `tests/e2e/fixtures.ts`：`no-empty-pattern` —— Playwright 要求 fixture 首参必须是对象解构，加注释后定点豁免。
- `scripts/post_package.cjs`：`.cjs` 中的 `require()` 被 TS 规则误报 → 该文件关闭 `no-require-imports`。

**类型收敛（warning 级，纯类型改动，运行时零影响）**

- `ffmpeg-service.ts`（33 → 0）：新增 `ScanEntry` / `InternalTask` / `InternalPlan` / `RunContext` / `ProcessMeta`
  等内部类型（用 type 别名以获得隐式索引签名），删除散落的 `as any` 与 `: any` 注解。
- `EngineEvent` 契约上移到 `shared/contracts.ts`，main 与 renderer 共用（`DesktopApi.onEngineEvent` 与 preload 同步收口）。
- `App.vue`（8 → 0）：5 处 `catch (…: any)` → `unknown` + `instanceof` 取值；引擎事件与 `summary` 改用 `EngineEvent`。
- `ffmpeg-environment.ts`（4 → 0）：新增 `HardwareCapabilities` 类型；GPU `generation` 归一化为契约要求的字符串。
- `ffmpeg-manifest.ts` / `plan.ts` / `ConfigPanel.vue` / `SettingsModal.vue`：`any` → 精确类型
  （`updateTaskStatus` 的 `newStatus` 改为 `TaskStatus`，并让 `updateTaskProgress/Status` 容忍缺失 `taskId`）。
- `main/index.ts`：IPC dispatcher 的 `(...args: any[])` 改为 `(...args: never[])` + 调用点一次显式断言，
  既消除 `any` 注解，也不把「渲染层入参已可信」写成类型事实。

**顺带补充 JSDoc（仅注释，运行时零影响）**：`ffmpeg_scan.js` 的 `ScanEntry` typedef 与
`scanDesktopInputFiles` 参数、`ffmpeg_planner.js` 的 `prepareFFmpegPlan` / `deleteCompletedSources` 参数，
使 JS facade 的类型推断正确，桌面端无需 cast 即可通过类型检查。

### 验证

- `npm run lint`（根 + 桌面端）：0 error / 0 warning；`npm run desktop:typecheck`：0 error；
  `npm test`：334/334；`npm run desktop:build`：成功。
- 桌面端 e2e 仍无法在本机运行（`electron.launch()` 报 `bad option: --remote-debugging-port=0`，
  属 Playwright/Electron 环境问题，与本次改动无关）。

---

## 2026-09-26 10:07 — ffmpeg 全链路评审问题修复（CLI + Electron + UI/UX）

依据 `docs/FFMPEG-REVIEW-20260926.md` 的评审结论，按「最小改动、不引入新行为」原则修复。

### 一、功能正确性（用户可感）

- **`--hwaccel cpu` 真正生效**（`src/transcode/hwdetect.js`）：`candidateTiers` 此前把 `cpu` 静默穿透到默认候选链，实测 `auto + hwaccel=cpu` 返回 `[cuda,swdec,d3d,cpu]`，与 `auto` 完全一致。现在 `cpu` 直接返回 `["cpu"]`（与 `--decode-mode cpu` 同义），CLI 与桌面端「硬件加速方式=cpu」不再是假开关。
- **桌面端「实时速度 / 剩余时间」恢复显示**（`src/transcode/ffmpeg_run.js`、`stores/plan.ts`）：`ffmpeg -progress` 的 `speed` 是 `"1.5x"` 字符串，而 store 用 `typeof === "number"` 判定，导致 `currentSpeed` 恒为 0、两个指标恒显示 `—`。现由执行层输出数值 `speed`（另附 `speedText`），store 侧同时接受数字/字符串以防御回归。
- **AMD 显卡不再丢失 AMF 硬编**（`src/transcode/gpu.js`、`hwdetect.js`）：`normalizeVendor` 新增 `radeon` 匹配（Windows 返回 `AMD Radeon ...` 型号串），`primaryVendor` 在 GPU 列表归一化为 `other` 时回退到设备探测得出的 `caps.vendor`。实测候选链由 `[d3d,cpu]` 恢复为 `[amf,swdec,d3d,cpu]`。
- **桌面端「移除所选」补齐状态同步**（`TaskTable.vue`）：底栏按钮此前只改 `planStore`，现与右键菜单共用同一实现，同步 `configStore.inputs` 与主进程 `stagedEntries`，并加 `isPlanBusy()` 守卫。
- **About 弹窗版本号改为真实值**（`AboutModal.vue`）：原先硬编码 `v0.1.0`，`envStore.version` 全渲染层无消费点。

### 二、状态语义

- **用户层预设可跨层 `extends` 内置预设**（`preset_loader.js`）：`resolveExtends` 增加 `base` 查找域，`loadPresetLayers` 逐层累积 base。此前 `extends: hevc_2k` 会 `Preset not found` 并整条丢弃。
- **运行期修改配置不再被静默吞掉**（`stores/plan.ts`、`App.vue`）：`markStale()` 在 RUNNING/PLANNING/STOPPING 时改为置 `pendingStale`，会话收敛时由 `applyPendingStale()` 兑现为 STALE；`startExecution` 的重推演条件纳入 `pendingStale`，消除「界面配置已改、实际执行旧 argv」的不一致。
- **重试时被重建判定为跳过的任务不再误标成功**（`ffmpeg_engine.js`）：`prepareAttempt` 合并后若 `status === "skipped"`，立即以 skipped 收尾并停止本任务（此前继续执行 ffmpeg，成功后又被 `toRunResult` 优先读成 skipped，统计与 UI 双错）。

### 三、文档与实现对齐

- **移除幽灵选项**：`--video-args` / `--audio-args` / `--filters` / `--filter-complex` 已在提交 `0b03adb` 中删除，但 README.md、README_zh.md、`docs/ffmpeg/FFMPEG-USAGE.md`、`presets.example.yaml`、`lib/i18n.js` 仍把它们当正式能力描述（实测 CLI 报 `Unknown arguments`）。本次统一改为「已移除 + 改用预设字段」的准确表述。
- 删除 5 个无消费者的 i18n 死键（`ffmpeg.video.args` / `ffmpeg.audio.args` / `ffmpeg.filters` / `ffmpeg.error.videoArgsCodec` / `ffmpeg.warn.encoderSpecific`）。
- 删除 `OPTION_KEYS` 中 4 个已无消费者的键（`filters` / `filterComplex` / `videoArgs` / `audioArgs`）及 `ffmpeg_build.js` 中读取 `audioArgs` 的 3 处死分支。
- 修正 6 处与实现矛盾的注释（`ffmpeg_build.js` ×3、`hwaccel.js` ×3、`ffmpeg_run.js` ×2、`ffmpeg_presets.js` ×2）。

### 四、工程健壮性与 UI/UX

- **库不再劫持宿主进程信号**（`ffmpeg_run.js`）：`SIGINT/SIGTERM` 处理（含 `process.exit(130)`）只在非 Electron 环境注册，避免库代码决定 GUI 退出。
- **`toSerializable` 改用路径栈**（`ipc-serializer.ts`）：共享 WeakSet 会把 DAG（同一对象被两字段引用）误判为 `[Circular]`；Set/Map 也纳入进出栈，消除自引用集合的无限递归。
- **探测缓存键纳入 `quality`**（`hwaccel.js`）：长驻进程跨批次换 `--video-quality` 不再复用旧结论；`bitrate/maxBitrate` 有意不入键（逐文件缩放值会让每文件成为独立键、缓存彻底失效，且码率不改变「该层能否打开文件」）。
- **`--error-file` 支持显式路径**（`ffmpeg_run.js`）：非 `json`/`text` 的取值视为文件路径（相对路径基于输出目录），多文件共用时追加而非覆盖；修复 JSON 模式下 `JSON.stringify(Error)` 得 `{}` 导致 message 丢失。
- **运行中禁止 reload**（`main/index.ts`）：`role: "reload"/"forceReload"` 改为带 `guardReload()` 的显式菜单项，避免渲染层重建后与引擎脱钩（进度丢失、无法终止）。
- **mediainfo 死控件接线**（`lib/mediainfo.js`、`ffmpeg-environment.ts`、`ffmpeg-service.ts`、`contracts.ts`、`App.vue`、`AboutModal.vue`）：设置里的 mediainfo 路径此前只被渲染层收集与持久化，主进程完全不消费；现作为 ffprobe 失败时的兜底探测工具真正传入。
- **输出目录一致性**（`ConfigPanel.vue`、`App.vue`、`ExecutionBoard.vue`）：目录选择器与手动输入统一走 `setCustomOutputDir`（修复 `savedCustomOutputDir` / `outputBesideSource` 不同步）；`openPath` 增加 catch 与日志，白名单拒绝不再静默。
- **推演命令诚实标注**（`TaskInspectorDrawer.vue`、`TaskTable.vue`、`stores/plan.ts`）：命令来源区分「计划推演 / 本地估算」并在估算时给出警示；替换基准由 `tasks[0]` 改为按 path 反查（`previewCmdFor`），修复移除过首行后复制出错误命令。
- **UI 细节**：FAILED 状态也显示「打开输出目录」；状态胶囊在 RUNNING/PLANNING/STOPPING 不再被「待规划」覆盖；设置面板开关补 Space/Enter 键盘激活；右键菜单支持 Shift+F10 / ContextMenu 呼出与键盘激活；并发数上限与文案对齐主进程真实行为（1–8，视频并非强制串行）。

### 验证

- `npm run lint` 0 错误；`npm test` 334/334 通过；`npm run desktop:typecheck` 0 错误；`npm run desktop:build` 成功（main/preload/renderer 三端产物均含本次改动）。
- CLI 端到端实测：`--hwaccel cpu` 产出 `-c:v libx265 -crf 23`（无 `-hwaccel cuda`）；`--error-file <绝对路径>.json` 写入指定路径且 `error.message` 完整保留。
- 跨层 `extends`、候选层链、引擎重试跳过语义均以脚本实测确认。
- 已知未覆盖：桌面端 e2e（9 例）在本机 `electron.launch()` 阶段即失败（`bad option: --remote-debugging-port=0`），沙箱开关两种模式表现一致，属 Playwright/Electron 环境问题，与本次改动无关。

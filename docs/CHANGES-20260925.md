# CHANGES 2026-09-25

## 2026-09-25 12:28 架构维护方案 R3.2：status 协议 done→success

- `createPublicTaskSnapshot()` 显式执行唯一映射 `internal done → public success`；内部
  Engine/plan 状态保持 `done` 不变（`ffmpeg_engine.js`、`ffmpeg_planner.js` 判终态仍用 done）。
- `shared/contracts.ts` 的 `TaskStatus` 删除 public `done`；renderer `plan.ts`
  TERMINAL/FINISHED/EXECUTED_STATUSES 与 progress 置 100 判断、`ExecutionBoard.vue` /
  `TaskTable.vue`（含 STATUS_MAP Record 类型）同步移除 done 兼容分支；main
  `ffmpeg-service.ts` 两处已完成/未完成过滤判断移除 done。
- `test/test_ffmpeg_snapshot.js` 新增「internal done 始终投影为 public success」用例。
- 验证：根 `npm test` 330/330（+1）、`check`、`lint` 通过；`desktop:typecheck`、
  `desktop:build`、`desktop:test:e2e` 6/6 通过。

## 2026-09-25 12:3x src/transcode 核心的 C#/.NET 10 移植版落地（dotnet/）

- 新增 `dotnet/` 子目录：`src/MediaCli.Transcode` 类库（预设/硬件分层/计划计算/命令拼装/执行/二进制定位）、
  `src/MediaCli.Transcode.Cli`（presets/plan/run，默认 dry-run，--doit 执行）、`tests/`（xUnit 59 项全通过）。
- 移植对照与已知差异见 `dotnet/docs/PORTING-NOTES.md`；JS 与 C# 对同一输入生成的
  ffmpeg 命令已验证逐字节一致（parity）；真机转码冒烟（hevc 10bit→hevc_2k、h264 422→h264_2k）均成功。
- 不改动任何 Node 版代码；Node 版仍为唯一行为基准。

## 2026-09-25 12:20 架构维护方案 R3.1：统一公共 task 投影

- `src/transcode/ffmpeg_plan_snapshot.js` 新增 `createPublicTaskSnapshot(taskOrEntry, index,
  defaultStatus = "pending")`，`createPublicPlanSnapshot()` 复用该函数；状态继承
  `task.status || defaultStatus`（staged 阶段保留 `"staged"`），内部字段（argv/preset/
  fileDstTemp/hwPlan 等）不泄漏，数值缺失回退 undefined（与 PlanTask optional 契约一致）。
- Electron `ffmpeg-service.ts`：`stageInputs()` 手工构造 PlanTask 改为经
  `createPublicTaskSnapshot` 投影；`createPlan()` 删除二次补 metadata 的循环（公开投影统一
  从 `task.info` 提取，buildTask 构建期已填充），仅保留 `stagedEntries` 重登记供
  isKnownMediaPath / 去重使用。
- facade 新增导出 `createPublicTaskSnapshot`。
- 新增 `test/test_ffmpeg_snapshot.js` 5 例：完整字段快照、内部字段隔离、defaultStatus 兜底、
  staged 与 plan task 元数据一致、createPublicPlanSnapshot 复用。
- 验证：根 `npm test` 329/329（+5）、`check`、`lint`（仓库代码干净）通过；`desktop:typecheck`、
  `desktop:build`、`desktop:test:e2e` 6/6 通过。

## 2026-09-25 12:07 架构维护方案 R2：统一 renderer 输入 ingestion

- 新增 `useInputIngest.ts` 唯一入口：保存输入（config store）→ IPC staging → 写入 Pinia
  plan store → 日志/错误处理，统一 App.vue、ConfigPanel.vue、GlobalDropMask.vue、HeroEmpty.vue
  四处的输入逻辑；各调用方不再各自调 `config.addInputs` / `stageInputs`。
- 去重一致性：以 config store 前后长度差计算本地去重数，与主进程 `skippedDuplicates` 取 max
  合并计数——不依赖主进程并发时序（并发触发时主进程 stagedEntries 可能尚未登记上一次请求，
  其去重会被绕过），重复时统一记录「跳过 N 个重复添加的文件」INFO 日志。
- 失败一致性：`stageInputs` 失败时 console.error + `planStore.markStale()` + 「暂存输入失败」
  ERROR 日志，config/plan/log 三 store 不会进入部分更新状态。
- 新增 E2E `04-ingest.spec.ts` 两个用例：① 同一路径串行添加两次，chip/task-row 仅 1 个、
  日志含「跳过 1 个重复添加的文件」；② `electronApp.evaluate` 中 removeHandler + handle 注入
  `ffmpeg:stage-inputs` 失败，断言 config 保留两路径、plan 保留 1 个成功任务且 state-tag 为
  「待规划」（hasStaged 优先）、日志含「暂存输入失败」、无 fatal renderer 错误。
- 验证：根 `npm test` 全绿、`check` 121、`lint` 通过；`desktop:typecheck`、`desktop:build`、
  `desktop:test:e2e` 6/6 通过（含新增 2 例）。

## 2026-09-25 11:55 架构维护方案 R1：Desktop 术语与历史注释清理

- 符号改名（仅重命名，不改行为）：`normalizeWebOptions` → `normalizeDesktopOptions`、
  `scanWebInputFiles` → `scanDesktopInputFiles`，内部私有函数 `pickTopLevelWebOptions` →
  `pickTopLevelDesktopOptions`；同步更新 `src/transcode/index.js` facade 导出、Electron
  `ffmpeg-service.ts` 引用与 `test_ffmpeg_contracts.js` / `test_ffmpeg_parity.js` /
  `test_ffmpeg_scan.js`。
- 测试名与临时文件去除已删除的 ffweb 语义：`CLI and ffweb...` → `CLI and desktop...`、
  `ffmpeg web scan...` → `ffmpeg desktop scan...`、`test_ffmpeg_web_scan_filelist.txt` →
  `test_ffmpeg_desktop_scan_filelist.txt`。
- 注释清理：`src/transcode/*.js` 中 WebUI/ffweb 措辞改为 Desktop；`hwaccel.js` 头部
  「草稿、尚未接入 cmd/cmd_ffmpeg.js」改为已接入事实说明，末尾历史「对接说明（尚未实施）」
  改为「设计背景与未完成项」并注明第 1-3 项已落地；`lib/media_parser.js`、`ffmpeg_build.js`、
  `hwdetect.js`、`preset_loader.js`、`preset_schema.js` 及 `presets/*.yaml` 中旧
  `lib/hwaccel.js`、`lib/preset_schema.js` 路径更正为 `src/transcode/*`。
- 验证：根 `npm test` 324/324、`check` 121 文件、`lint` 通过、`desktop:typecheck`、
  `desktop:build`、`desktop:test:e2e` 4/4 通过。

## 2026-09-25 11:50 架构维护方案 R0：边界守卫与转码 facade 收口

- 新增维护方案 `docs/ARCHITECTURE-MAINTENANCE-PLAN-20260925.md`（v1.2，含第二轮核对修正：重复语句、
  `buildCliTask` 事实描述、R1 改名前提、R0.4 实际影响范围）。
- 边界守卫扩展（`test/test_architecture_boundaries.js`，320 → 324）：新增 `lib/` 不得 import
  `cmd/`、`src/`、`apps/`；Electron main 不得 import `cmd/`；Electron renderer/preload 不得 import
  根 `src/`、`lib/`、`cmd/`；Electron main 不得直连 `lib/mediainfo.js`。
- transcode facade（`src/transcode/index.js`）新增导出 `getMediaInfo`，`ffmpeg-service.ts` 改为经
  facade 导入，切断 Desktop 对 `lib/mediainfo.js` 的直接穿透。
- 根 `package.json` 新增桌面端统一入口：`desktop:dev|typecheck|build|test:e2e|package:win`。
- `scripts/check_syntax.cjs` 跳过 `out/`、`release/`、`test-results/`，扫描文件 125 → 121。
- `AGENTS.md` 补充依赖边界守卫与桌面端入口说明。
- 验证：根 `npm test` 324/324、`check` 121 文件、`lint` 通过、`desktop:typecheck` 与 `desktop:build` 通过。

## 2026-09-25 11:07 迁移汉字字符集资源

- 将五组汉字字符集从 `lib/*.txt` 迁移为 `assets/hanzi/*.json`。
- `lib/unicode.js` 改用 JSON 静态导入，移除模块加载阶段的文件读取。
- Electron 构建移除汉字 TXT 固定清单及复制逻辑，字符集随主进程 bundle 打包。

## 2026-09-25 10:33 启动转码领域目录重构

- 新增 `src/transcode/` 作为 ffmpeg/transcode 实现边界，CLI 与 Electron 统一经 facade 导入。
- 18 个转码模块由 `lib/` 平移至 `src/transcode/`，共享依赖继续复用 `lib/`。
- 新增架构边界守卫和 npm tarball 安装态 smoke，Node.js 最低版本统一提升至 22。
- 验证通过：根项目 320/320、lint/check/package smoke、Electron typecheck/build/e2e 4/4；Windows NSIS/portable 产物已从仓库外 cwd 完成无 FFmpeg 降级与真实转码 smoke。

## 2026-09-25 09:31 修复 Unfixed-Issues-20260925 第二批（#26/#17/#28/#14/#5/#3/#33/#29/#4+#34+#9）

九项渲染层/引擎侧小影响面修复：

- **#26** 死代码删除：`stores/log.ts` addLog、`stores/plan.ts` planningProgress（全仓无调用）。
- **#17** getTaskSnapshot 死接口链整体移除：service 方法 + `main/index.ts` handler +
  `preload` 暴露 + `shared/contracts.ts` 声明 + `ipc-channels.ts` EXECUTION_SNAPSHOT 通道。
- **#28** `TaskTable.vue` getFmtClass 兜底返回值 `fmt-default`→`fmt-other`（CSS 只有后者）。
- **#14** `lib/ffmpeg_run.js` 产物过小/无产物分支：原只打日志不返回（调用方拿 undefined，
  toRunResult 报无信息量的 "FFmpeg returned no result"）；现 return entry 并在 ffmpegError
  写明根因（unexpectedly small / missing + 源大小）。刻意不设 ffmpegFailed，避免误入 CPU 解码重试链路。
- **#5** `App.vue` createPlan：deleteSourceConfirmed 改为反映本次高危 confirm 弹窗的真实
  确认结果（此前直接复用 deleteSource 开关，主进程校验形同虚设）。
- **#3** `ConfigPanel.vue` dropzone `@drop` 加 `.stop`，消除与 GlobalDropMask window 级
  drop 监听的双路并发 stageInputs 竞态。
- **#33** `stores/log.ts`：errCount 改为从缓冲区 computed 派生（滚动淘汰 ERROR 后计数同步收敛）；
  taskId 文本匹配改词边界正则（task_123 不再误命中 task_1234）。
- **#29** `SettingsModal.vue` 常驻挂载，watch show 打开时重读 data-theme，修复切主题后显示旧值。
- **#4+#34+#9** `stores/plan.ts` + `ExecutionBoard.vue` 进度统计重构：
  - updateTaskProgress/updateTaskStatus 加终态守卫，乱序事件不再复活已完成任务；
  - overallPercent 改按「本轮实际执行子集」（running/success/done/failed/skipped/cancelled）
    时长加权聚合，无时长信息退化为按个数；部分执行时进度条不再被未选中任务稀释；
  - ETA 改用 executedDuration（子集时长），不再用全量 planSnapshot.totalDuration 折算；
  - STOPPED/FAILED 时进度条冻结在实际达到值（与完成数口径一致，不再归零）；
  - overallStat 分母改用执行子集（"2 / 3" 而非误导性的 "2 / 10"）。

验证：根项目 npm test 318/318、lint 0 错误、desktop typecheck 通过、desktop e2e 4/4 全过
（含真实转码全流程）；加权进度/终态守卫/词边界匹配逻辑脚本级验证通过。
另确认：此前疑似 failed 的 `node --test test/` 为 Node 24 目录参数解析怪癖
（把 test/ 当模块加载报 MODULE_NOT_FOUND），非真实测试失败，npm test 标准入口无此问题。

## 2026-09-25 09:26 新增目录结构重构合并终稿

评审 `REFACTOR-STRUCTURE-PLAN-20260924.md`、`-FINAL-20260925.md`、`-SIMPLE-20260925-qwm.md`
三份方案，逐条对仓库实测后输出合并终稿 `docs/REFACTOR-STRUCTURE-PLAN-MERGED-20260925.md`（无代码改动）：

- 采 v2 骨架（单发布单元、`src/` 普通目录、facade 前置、机械迁移+冻结垃圾桶）+ v1 的按职责分目录与发布门禁；
- 否决 20260924 全案（相对导入+空壳 package.json 自相矛盾、基线过时）；
- 否决 v1 的 core/helper 粉碎拆分（实测需重接 87 个导出）与 18 个模块全量改名；
- 修正 5 处实测问题：`node --test test` 在 Node 24 下 MODULE_NOT_FOUND（应为 `node --test "test/**/*.js"`，实测 318/318）、
  `preset_loader.js` 相对 preset 路径随搬迁失效、守卫扫描范围自相矛盾、labs 两个 CJS 死文件会致守卫误报、工作区需先清干净。

## 2026-09-25 09:15 修复 Unfixed-Issues-20260925 第一批（#13/#10/#11/#16/#36）

依据 `docs/Unfixed-Issues-20260925.md` 实施五项低风险高收益修复：

- **#13 L-11** `lib/ffmpeg_run.js` resolveHwPlan：GPU 分支 throw 后又无条件 throw 的冗余 if，
  经 ESLint no-useless-catch 确认后整体移除 try/catch 包装，错误语义不变（一律上抛）。
- **#10 S-1** desktop 主进程 `SYSTEM_OPEN_PATH` / `SYSTEM_SHOW_IN_FOLDER` 路径白名单加固：
  - `ffmpeg-service.ts` 新增 `authorizedRoots`（持久化于 userData `authorized-paths.json`，跨会话）、
    `authorizePaths()`、`isKnownMediaPath()`——白名单 = staged 输入 ∪ 计划任务源/产物 ∪ 已知文件直接父目录 ∪ 对话框授权根子树；
  - `index.ts`：`DIALOG_SELECT_FILES` 返回的路径即登记授权；两个 SYSTEM 通道入口做白名单校验，非已知路径直接拒绝。
- **#11 S-2** `ffmpeg-service.ts` `isManagedTempPath` 仅凭文件名子串可删的启发式，升级为
  `isManagedTempEntry` 结构化校验：manifest 的 tempPath 与 outputPath 必须同目录（win32 大小写不敏感）、
  同扩展名，且文件名严格匹配 `xxx_tmp@<hex>@tmp_.ext`；不满足即跳过删除（宁残留勿误删）。
- **#16 P2-6** `ffmpeg-service.ts` createPlan：除 RUNNING 外同时拦截 STOPPING，防止停止收尾期间
  重建计划清空 currentPlan 与 stopExecution 收尾写入互相踩踏。
- **#36 T-3** `tests/e2e/playwright.config.ts`：`retries: 0 → 1`，配合已有的 `trace: "on-first-retry"`
  缓解 Electron e2e 偶发超时导致的假红。

验证：根项目 `npm test` 318/318 通过、`npm run lint` 0 错误、desktop `npm run typecheck` 通过；
isManagedTempEntry 边界用例（同目录/跨目录/扩展名/伪哈希/相对路径/子串伪 temp）8/8 通过。
